---

- name: install kibana dependencies
  sudo: true
  sudo_user: app
  npm: path=/data/kibana

- name: build kibana
  sudo: true
  sudo_user: app
  command: grunt build chdir=/data/kibana

- name: restart kinesis agent
  sudo: true
  service: name=aws-kinesis-agent state=restarted
